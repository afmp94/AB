<% cache(["task_destroy", activity, current_user.id]) do %>
  <div class="activity_container">
    <article class="activity">
    <%= activity_icon("info", "checkmark icon") %>
      <div class="activity-flat-container">
        <div class="activity-flat">
          <%= render 'public_activity/shared/activity_timestamp', activity: activity %>
          <div class="action">
            <%= activity_creator(activity) %>
            <%= activity_subject(activity, action: "deleted the task", name: true) %>
            <%= activity_bucket(activity, "from", recipient_name(activity), activity_link(activity.recipient)) %>
          </div><!-- /.action -->
        </div><!-- /.activity-flat -->
      </div><!-- /.activity-flat-container -->
    </article><!-- /.activity -->
  </div><!-- /.activity_container -->
<% end %>
