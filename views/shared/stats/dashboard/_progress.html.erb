<div class="row well dashboard-progress relative">
  <div class="col-xs-12">
    <h3>Today's Progress:</h3>
    <ul class="nav nav-pills below18 team-only">
      <li role="presentation" class="active"><a href="#my-progress" data-toggle="pill">My Progress</a></li>
      <li role="presentation"><a href="#team-progress" data-toggle="pill">Team Progress</a></li>
    </ul>
    <div class="row">
      <div class="col-xs-12">
        <div class="tab-content nopadding noborder">
          <div class="tab-pane fade in active" id="my-progress">
            <%= stat_block(
              value: @dashboard_presenter.dashboard_carrier.open_leads_count,
              label: "Leads Needing Action",
              columns: 4
            ) %>
            <%= stat_block(
              value: @dashboard_presenter.dashboard_carrier.clients_without_next_action_count,
              label: "Clients Without Next Actions",
              columns: 4
            ) %>
            <%= stat_block(
              value: "#{@dashboard_presenter.dashboard_carrier.todays_task_progress_completed}/#{@dashboard_presenter.dashboard_carrier.todays_task_progress_total}",
              label: "Tasks Completed",
              columns: 4
            ) %>
          </div>
          <div class="tab-pane fade" id="team-progress">
            <%= stat_block(
              value: @dashboard_presenter.dashboard_carrier.team_clients_without_next_action_count,
              label: "Clients Without Next Actions",
              columns: 6
            ) %>
            <%= stat_block(
              value: "#{@dashboard_presenter.dashboard_carrier.todays_team_task_progress_completed}/#{@dashboard_presenter.dashboard_carrier.todays_team_task_progress_total}",
              label: "Tasks Completed",
              columns: 6
            ) %>
          </div>
        </div>
      </div><!-- /.col-xs-12 -->
    </div><!-- /.row -->
  </div><!-- /.col-xs-12 -->
</div><!-- /.row well -->
